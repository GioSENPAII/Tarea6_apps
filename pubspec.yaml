name: ar_flutter_app
description: "A new Flutter project for AR functionality."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  ar_flutter_plugin: ^0.7.3
  permission_handler: ^10.4.5
  http: ^1.1.0
  path_provider: ^2.1.1
  vector_math: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/